package practice.Geometry.cubeSurfaceArea;

public class cube {

    public static double area(double width, double length, double height) {
        return (2 * length * width) + (2 * length * height) + (2 * height * width);
    }
}
